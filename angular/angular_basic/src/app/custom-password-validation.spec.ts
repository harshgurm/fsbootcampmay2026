import { CustomPasswordValidation } from './custom-password-validation';

describe('CustomPasswordValidation', () => {
  it('should create an instance', () => {
    expect(new CustomPasswordValidation()).toBeTruthy();
  });
});
